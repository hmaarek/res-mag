<p id="notice"><%= notice %></p>

<h1>Listing Cables</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Location a</th>
      <th>Location b</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @cables.each do |cable| %>
      <tr>
        <td><%= cable.name %></td>
        <td><%= cable.location_a_id %></td>
        <td><%= cable.location_b_id %></td>
        <td><%= link_to 'Show', cable %></td>
        <td><%= link_to 'Edit', edit_cable_path(cable) %></td>
        <td><%= link_to 'Destroy', cable, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Cable', new_cable_path %>
