<%= form_for(@cable) do |f| %>
  <% if @cable.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@cable.errors.count, "error") %> prohibited this cable from being saved:</h2>

      <ul>
      <% @cable.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :location_a_id %><br>
    <%= f.number_field :location_a_id %>
  </div>
  <div class="field">
    <%= f.label :location_b_id %><br>
    <%= f.number_field :location_b_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
